apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: ingress7
spec:
  rules:
  - host: foo.bar.com
    http:
      paths:
      - backend:
          serviceName: k8test1
          servicePort: 81
  - host: bar.foo.com
    http:
      paths:
      - backend:
          serviceName: k8test2
          servicePort: 80

